
{
  "entities": {
    "Product": {
      "title": "Producto Inteligente",
      "description": "Un producto con metadatos de IoT y eficiencia energética.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "price": { "type": "number" },
        "quantity": { "type": "number" },
        "imageUrl": { "type": "string" },
        "category": { "type": "string" },
        "energyRating": { "type": "string", "enum": ["A+++", "A++", "A", "B", "C"] },
        "isIotEnabled": { "type": "boolean" },
        "arrivalDate": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "price", "quantity", "imageUrl"]
    },
    "SaleRecord": {
      "title": "Registro Logístico",
      "description": "Venta con seguimiento IoT.",
      "type": "object",
      "properties": {
        "saleDate": { "type": "string", "format": "date-time" },
        "totalAmount": { "type": "number" },
        "userId": { "type": "string" },
        "shippingStatus": { "type": "string", "enum": ["in_transit", "delivered", "processing"] },
        "itemsSold": { "type": "array" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "products/{productId}": { "schema": { "$ref": "#/entities/Product" }, "description": "Stock inteligente." },
    "sales/{saleId}": { "schema": { "$ref": "#/entities/SaleRecord" }, "description": "Historial logístico." }
  }
}
